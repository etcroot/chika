const config = require('../../config.json');
const Discord = require('discord.js');

module.exports = {
   name: 'eval',
   description: 'Eval.',
   execute: async (client, message, args) => {
   // soon??
    }
   }